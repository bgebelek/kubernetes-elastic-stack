apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kb-domain-certificate
spec:
  secretName: kb-domain-tls-secret
  duration: 8760h
  renewBefore: 360h
  isCA: false
  dnsNames:
    - kibana.elastic.com
  issuerRef:
    name: ca-issuer
