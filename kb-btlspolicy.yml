apiVersion: gateway.networking.k8s.io/v1
kind: BackendTLSPolicy
metadata:
  name: kb-btlspolicy
spec:
  targetRefs:
    - name: kb-clusterip-service
      kind: Service
      group: ''
  validation:
    caCertificateRefs:
      - name: ca-configmap
        kind: ConfigMap
        group: ''
    hostname: kb-clusterip-service

