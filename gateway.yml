apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: gateway
spec:
  gatewayClassName: traefik
  listeners:
    - name: flt-listener
      port: 8220
      protocol: HTTPS
      tls:
        
