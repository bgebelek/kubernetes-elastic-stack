apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: es-domain-certificate
spec:
  secretName: es-domain-tls-secret
  duration: 8760h
  renewBefore: 360h
  isCA: false
  dnsNames:
    - elasticsearch.elastic.com
  issuerRef:
    name: ca-issuer
