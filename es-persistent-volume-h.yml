apiVersion: v1
kind: PersistentVolume
metadata:
  name: es-persistent-volume-h
spec:
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/es-persistent-volume-h
  storageClassName: es-persistent-volume
  capacity:
    storage: 5Gi
