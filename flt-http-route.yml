apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: flt-http-route
spec:
  hostnames:
    - 'fleet-server.elastic.com'
  parentRefs:
    - kind: Gateway
      name: elastic-gateway
  rules:
    - backendRefs:
        - name: flt-clusterip-service
          port: 8220
