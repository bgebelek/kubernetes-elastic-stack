apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: flt-service-certificate
spec:
  secretName: flt-service-tls-secret
  duration: 8760h
  renewBefore: 360h
  isCA: false
  dnsNames:
    - flt-clusterip-service
  issuerRef:
    name: ca-issuer
